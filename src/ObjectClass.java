public class ObjectClass {
    /** 15. Object class
    # class bernama "Object" yg ada di java,jadi setiap class yang kita buat di java secara otomatis adalah turunan
      dari class Object
    # walaupun tidak secara langsung kita eksplisit menyebutkan keyword extends Object, tapi secara otomatis java
      akan membuat class kita etends ke class Object (menjadi child dari class Object, namun apabila sebuah class extend
      ke class lain maka class tersebut akan menjadi child dari class parentnya dan parentnya baru akan extend ke object )
    # Bisa dikatakan class Object adalah superclass untuk semua class yang ada di java*/

    //untuk melihat class object silahkan klik navigate -> class -> pilih Object java.lang
    // jadi semua method dan field yang ada diclass Object dapat kita akses/panggil didalam class class yang kita buat


}
