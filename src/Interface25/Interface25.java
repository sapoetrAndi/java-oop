package Interface25;

public class Interface25 {
    /** 25. Interface
    # sebelumnya kita sudah tahu bahwa abstract class bisa kita gunakanan sebagai kontrak(class untuk di turunkan)
      untuk class childnya
    # Namun sebenarnya yang lebih tepat untuk kontrak adalah Interface
    # jangan salah sangka bahwa INterface disini bukanlah user interface
    # interface mirip seperti abstract class, yang membedakan adalah di interface, semua method otomatis abstract,
      tidak memiliki block
    # Di interface kita tidak boleh memiliki field, kita hanya boleh memiliki constant (field yang tidak bisa diubah)
    # untuk mewariskan interface, kita tidak menggunakan kata kunci extends, melainkan implements*/


    //implementasi ada di interface car, class Avanza, dan class,CarApp


}
